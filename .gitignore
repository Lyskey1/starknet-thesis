node_modules/
.DS_Store
.vercel
.netlify
*.log

# local tooling / environment
.claude/
.env
.env.*

# regenerable cache written by scripts/backfill-staking.js
scripts/.backfill-cache.json

# old local archive snapshot — not for publication
starknet-thesis.zip

# local video assets: 347MB total, and eli-4.mp4 (121MB) exceeds GitHub's
# 100MB per-file hard limit — host separately or move to Git LFS. The site's
# video slots fall back gracefully when files are absent.
videos/
