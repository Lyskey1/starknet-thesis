{
  "name": "starknet-thesis",
  "version": "0.1.0",
  "private": true,
  "description": "Starknet x Bitcoin thesis website",
  "scripts": {
    "start": "npx serve",
    "dev": "python3 -m http.server 8000",
    "build": "node scripts/build-privacy-matrix.js && node scripts/check-ecosystem-orbits.js && node scripts/check-quoted-definition.js && node scripts/build-digest.js && node scripts/build-search-index.js"
  }
}
