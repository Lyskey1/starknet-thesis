/* Q-DAY TARGET, one constant (2026-09-08). The industry's converging
   deadline the quantum page counts down to (Microsoft, Google and IBM all
   name 2029; see the cards in quantum's section 01). quantum.html's
   countdown reads it here and derives its displayed year from it; the
   landing's FAQ reads this file at build (src/lib/data/qday.ts) so the two
   pages cannot state different years. Change the date HERE only. */
window.QDAY_TARGET_ISO = '2029-01-01T00:00:00Z';
